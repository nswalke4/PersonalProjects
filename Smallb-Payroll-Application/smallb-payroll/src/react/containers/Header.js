import React from "react";

/* The Header container contains all of the Header information: the app name, the ability to log out of the app, and other
important information to be available to the user on all views of the application. */
const Header = () => {
    return (
        <div className="header">
            <h1>Small-b Payroll</h1>
            <h2>Prototype Design</h2>
            <hr />
        </div>
    );
};

export default Header;

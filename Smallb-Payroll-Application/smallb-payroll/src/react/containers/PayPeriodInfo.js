import React from "react";

const PayPeriodInfo = ({ onClose, perId }) => {
    return (
        <div>
            <p>Pay Period Information will go here...</p>
            <button onClick={onClose}>Close</button>
        </div>
    );
};

export default PayPeriodInfo;
